{
  "name": "secretlink",
  "version": "0.19.2",
  "private": true,
  "description": "SecretLink monorepo - version source of truth",
  "author": "Nicolas BARRAT",
  "engines": {
    "node": ">=24 <25",
    "npm": ">=10"
  },
  "scripts": {
    "version": "node scripts/sync-version.mjs && git add client/package.json client/package-lock.json server/package.json server/package-lock.json",
    "version:sync": "node scripts/sync-version.mjs",
    "version:check": "node scripts/sync-version.mjs --check",
    "db:test:up": "docker compose -f deploy/docker-compose.test.yml up -d --wait db",
    "db:test:down": "docker compose -f deploy/docker-compose.test.yml down -v",
    "test:server": "npm --prefix server run test",
    "test:client": "npm --prefix client run test",
    "usine": "npm run version:check && npm run db:test:up && npm run test:server && npm run test:client && npm run db:test:down"
  }
}
